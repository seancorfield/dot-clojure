#!/bin/sh

if test -f .nrepl-port
then
  if test "$1" = "force"
  then
    shift
  else
    echo ".nrepl-port exists: $(cat .nrepl-port)"
    exit 1
  fi
fi

clojure "$@" -M:1.12:portal:test:cider-nrepl:rebel:dev/repl
